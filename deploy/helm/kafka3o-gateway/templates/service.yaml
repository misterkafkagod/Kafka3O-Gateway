apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka3o-gateway.fullname" . }}
  labels:
    {{- include "kafka3o-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kafka3o-gateway.selectorLabels" . | nindent 4 }}
